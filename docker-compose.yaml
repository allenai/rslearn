services:
  fake-gcs-server:
    image: fsouza/fake-gcs-server:1.47.8
    command:
      - "-scheme"
      - "http"
      - "-backend"
      - "memory"
      - "-public-host"
      - "localhost:4443"
    volumes:
      - ./test-data/fakegcs:/data
    restart: unless-stopped
    ports:
      - "4443:4443"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -f http://localhost:4443/ || exit 1"
        ]
      interval: 2s
      timeout: 2s
      retries: 120
